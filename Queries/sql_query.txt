Initial csv files have been merged on the primary key (VAERS_ID) using pandas, and added to a pgadmin database (VAERS_DATA).
A table has been succesfully created, and failed error received when importing data. 

SQL QUERY

-- Creating table for VAERS_DATA(without symptom and vax csv data) TABLE 1
CREATE TABLE VAERS_DATA (
	 INDEX1 INT,
     VAERS_ID VARCHAR NOT NULL,
     RECVDATE TEXT, 
	 STATE_ VARCHAR (2),
	 AGE_YRS FLOAT, 
	 CAGE_YR FLOAT, 
	 SEX VARCHAR,
	 SYMPTOM_TEXT TEXT,
	 DIED VARCHAR,
	 DATEDIED TEXT,
	 L_THREAT VARCHAR,
	 ER_VISIT VARCHAR,
	 HOSPITAL VARCHAR,
	 HOSPDAYS FLOAT,
	 X_STAY VARCHAR,
	 DISABLE_ VARCHAR, 
	 RECOVD VARCHAR,
	 VAX_DATE TEXT,
	 ONSET_DATE TEXT,
	 NUMDAYS FLOAT,
	 LAB_DATA VARCHAR, 
	 V_ADMINBY VARCHAR,
	 V_FUNDBY VARCHAR,
	 OTHER_MEDS TEXT,
	 CUR_ILL TEXT, 
	 HISTORY TEXT,
	 PRIOR_VAX VARCHAR,
	 SPLTTYPE VARCHAR,
	 FORM_VERS FLOAT,
	 TODAYS_DATE TEXT, 
	 BIRTH_DEFECT VARCHAR,
	 OFC_VISIT VARCHAR, 
	 ER_ED_VISIT VARCHAR, 
	 ALLERGIES TEXT,
	 
	
	 
     PRIMARY KEY (VAERS_ID),
     UNIQUE (VAERS_ID)
);


TABLE 2

CREATE TABLE VAERS_SYMPT_VAX (
	 VAERS_ID VARCHAR NOT NULL,
	 SYMPTOM1 VARCHAR,
	 SYMPTOMVERSION1 INT,
	 SYMPTOM2 VARCHAR,
	 SYMPTOMVERSION2 INT,
	 SYMPTOM3 VARCHAR,
	 SYMPTOMVERSION3 INT, 
	 SYMPTOM4 VARCHAR,
	 SYMPTOMVERSION4 INT, 
	 SYMPTOM5 VARCHAR,
	 SYMPTOMVERSION5 INT,
	 VAX_TYPE VARCHAR,
	 VAX_MANU VARCHAR, 
	 VAX_LOT VARCHAR, 
	 VAX_DOSE_SERIES INT,
	 VAX_ROUTE VARCHAR,
	 VAX_SITE VARCHAR, 
	 VAX_NAME VARCHAR,
	 PRIMARY KEY (VAERS_ID),
     UNIQUE (VAERS_ID)
	
	);