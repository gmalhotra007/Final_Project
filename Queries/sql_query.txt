Initial csv files have been merged on the primary key (VAERS_ID) using pandas, and added to a pgadmin database (VAERS_DATA).
A table has been succesfully created, and failed error received when importing data. 

SQL QUERY

-- Creating table for VAERS_DATA(vaers data merged using pandas)
CREATE TABLE VAERS_DATA (
     VAERS_ID VARCHAR NOT NULL,
     RECVDATE DATE,
	 _STATE VARCHAR (2),
	 AGE_YRS INT, 
	 CAGE_YR INT, 
	 SEX VARCHAR,
	 SYMPTOM_TEXT VARCHAR,
	 DIED VARCHAR,
	 DATEDIED DATE,
	 L_THREAT VARCHAR,
	 ER_VISIT VARCHAR,
	 HOSPITAL VARCHAR,
	 HOSPDAYS INT,
	 X_STAY VARCHAR,
	 DISABLE VARCHAR, 
	 RECOVD VARCHAR,
	 VAX_DATE DATE,
	 ONSET_DATE DATE,
	 NUMDAYS INT,
	 LAB_DATA VARCHAR, 
	 V_ADMINBY VARCHAR,
	 V_FUNDBY VARCHAR,
	 OTHER_MEDS VARCHAR,
	 CUR_ILL VARCHAR, 
	 HISTORY VARCHAR,
	 PRIOR_VAX VARCHAR,
	 SPLTTYPE VARCHAR,
	 FORM_VERS INT,
	 TODAYS_DATE DATE, 
	 BIRTH_DEFECT VARCHAR,
	 OFC_VISIT VARCHAR, 
	 ER_ED_VISIT VARCHAR, 
	 ALLERGIES VARCHAR,
	 SYMPTOM1 VARCHAR,
	 SYMPTOMVERSION1 INT,
	 SYMPTOM2 VARCHAR,
	 SYMPTOMVERSION2 INT,
	 SYMPTOM3 VARCHAR,
	 SYMPTOMVERSION3 INT, 
	 SYMPTOM4 VARCHAR,
	 SYMPTOMVERSION4 INT, 
	 SYMPTOM5 VARCHAR,
	 SYMPTOMVERSION5 INT,
	 VAX_TYPE VARCHAR,
	 VAX_MANU VARCHAR, 
	 VAX_LOT VARCHAR, 
	 VAX_DOSE_SERIES INT,
	 VAX_ROUTE VARCHAR,
	 VAX_SITE VARCHAR, 
	 VAX_NAME VARCHAR,
	 
	 
	 
	 
     PRIMARY KEY (VAERS_ID),
     UNIQUE (VAERS_ID)
);





-- Creating table for VAERS_DATA(vaers data merged using pandas)
CREATE TABLE VAERS_DATA (
     VAERS_ID VARCHAR NOT NULL,
     RECVDATE DATE,
	 _STATE VARCHAR (2),
	 AGE_YRS INT, 
	 CAGE_YR INT, 
	 SEX VARCHAR,
	 SYMPTOM_TEXT VARCHAR,
	 DIED VARCHAR,
	 DATEDIED DATE,
	 L_THREAT VARCHAR,
	 ER_VISIT VARCHAR,
	 HOSPITAL VARCHAR,
	 HOSPDAYS INT,
	 X_STAY VARCHAR,
	 DISABLE VARCHAR, 
	 RECOVD VARCHAR,
	 VAX_DATE DATE,
	 ONSET_DATE DATE,
	 NUMDAYS INT,
	 LAB_DATA VARCHAR, 
	 V_ADMINBY VARCHAR,
	 V_FUNDBY VARCHAR,
	 OTHER_MEDS VARCHAR,
	 CUR_ILL VARCHAR, 
	 HISTORY VARCHAR,
	 PRIOR_VAX VARCHAR,
	 SPLTTYPE VARCHAR,
	 FORM_VERS INT,
	 TODAYS_DATE DATE, 
	 BIRTH_DEFECT VARCHAR,
	 OFC_VISIT VARCHAR, 
	 ER_ED_VISIT VARCHAR, 
	 ALLERGIES VARCHAR,
	 
	 
	 
     PRIMARY KEY (VAERS_ID),
     UNIQUE (VAERS_ID)
);


CREATE TABLE VAERS_SYMPT_VAX (
	 VAERS_ID VARCHAR NOT NULL,
	 SYMPTOM1 VARCHAR,
	 SYMPTOMVERSION1 INT,
	 SYMPTOM2 VARCHAR,
	 SYMPTOMVERSION2 INT,
	 SYMPTOM3 VARCHAR,
	 SYMPTOMVERSION3 INT, 
	 SYMPTOM4 VARCHAR,
	 SYMPTOMVERSION4 INT, 
	 SYMPTOM5 VARCHAR,
	 SYMPTOMVERSION5 INT,
	 VAX_TYPE VARCHAR,
	 VAX_MANU VARCHAR, 
	 VAX_LOT VARCHAR, 
	 VAX_DOSE_SERIES INT,
	 VAX_ROUTE VARCHAR,
	 VAX_SITE VARCHAR, 
	 VAX_NAME VARCHAR,
	 PRIMARY KEY (VAERS_ID),
     UNIQUE (VAERS_ID)
	
	);